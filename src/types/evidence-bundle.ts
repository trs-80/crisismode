// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 CrisisMode Contributors

// Re-exported from @crisismode/agent-sdk — the canonical definition (with doc
// comments) lives at packages/agent-sdk/src/types/evidence-bundle.ts. This shim
// preserves existing '../types/evidence-bundle.js' import paths.
export type {
  EvidenceSourceKind,
  EvidenceContentType,
  EvidenceCollectionMode,
  EvidenceExecutionMode,
  ActionClass,
  PrimitiveMetadata,
  EvidenceContent,
  EvidenceItem,
  RequestVisibility,
  ActionPolicy,
  OutputContractSection,
  OutputContract,
  AdapterRequest,
  HypothesisConfidence,
  HypothesisType,
  Hypothesis,
  EvidenceRelevance,
  EvidenceReference,
  NextStepPurpose,
  NextStep,
  MutationType,
  ProposedAction,
  Abstention,
  Uncertainty,
  ArtifactKind,
  ArtifactRef,
  AgentMetadata,
  AdapterResponseState,
  AdapterResponse,
  AdapterExchange,
} from '@crisismode/agent-sdk';
